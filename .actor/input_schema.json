{
    "title": "Aziende.it Scraper — imprese per categoria ATECO",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrls": {
            "title": "URL categorie ATECO",
            "type": "array",
            "description": "Una o più pagine-categoria di aziende.it, es: https://www.aziende.it/categorie/servizi-di-informazione-e-comunicazione/62/62.02.0",
            "editor": "stringList",
            "default": ["https://www.aziende.it/categorie/servizi-di-informazione-e-comunicazione/62/62.02.0"]
        },
        "maxItems": {
            "title": "Max risultati totali",
            "type": "integer",
            "description": "Numero massimo di aziende da salvare.",
            "default": 5000,
            "minimum": 1,
            "maximum": 500000
        },
        "maxPagesPerCategory": {
            "title": "Max pagine per categoria",
            "type": "integer",
            "description": "Limite di pagine (?pag=N) seguite per ciascun URL categoria.",
            "default": 10,
            "minimum": 1,
            "maximum": 5000
        },
        "debug": {
            "title": "Debug (dump HTML)",
            "type": "boolean",
            "description": "Salva l'HTML della prima pagina nel Key-Value Store per verificare i selettori.",
            "default": true
        },
        "proxyConfig": {
            "title": "Proxy",
            "type": "object",
            "editor": "proxy",
            "default": { "useApifyProxy": true }
        }
    },
    "required": ["startUrls"]
}
